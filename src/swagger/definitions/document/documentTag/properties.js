import { type as T, format } from '../../../constants'


export const id = {
	description: 'id',
	type: T.integer,
	example: 10000000,
}

export const documentId = {
	description: 'documentId',
	type: T.integer,
	example: 1,
}

export const tagId = {
	description: 'tagId',
	type: T.integer,
	example: 1,
}

